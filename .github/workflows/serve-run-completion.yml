name: Serve Run Completion (Claude planner, live HTTP)

# Boots `aindy-runtime serve` in the app profile against a live Postgres/Redis/Mongo
# stack and drives ONE real agent run end-to-end over HTTP with the Claude planner
# (AINDY_AGENT_PLANNER_BACKEND=anthropic_chat): register -> create (Claude plans) ->
# approve -> execute -> assert the run reaches `completed` with steps.
#
# This is the Linux-authoritative version of the planner spike (Tier 2). It cannot be
# done reliably on a Windows/Docker-Desktop dev box: the runtime's /api/version handler
# deadlocks shelling out to `docker info` (its 3s timeout can't fire on Windows because
# Docker Desktop keeps the pipe open), and the Docker Desktop port-forward makes each pg
# connection ~4s so the pool exhausts. On Linux the sandbox probe skips the subprocess
# entirely and pg is sub-ms, so both problems vanish.
#
# Requires repo secrets: ANTHROPIC_API_KEY (planner), OPENAI_API_KEY (embeddings),
# DEEPSEEK_API_KEY (arm). Manual only — costs one Anthropic call per run.

on:
  workflow_dispatch:
    inputs:
      planner_backend:
        description: "Planner backend. anthropic_chat needs Anthropic egress (blocked on GitHub-hosted runners, RTR-1-NODUS-APPTOOL-500). runtime_local needs no egress."
        type: choice
        default: anthropic_chat
        options:
          - anthropic_chat
          - runtime_local
      objective:
        description: "Run objective. For runtime_local use a note/record phrasing (e.g. 'Record a note: prep my launch') -> memory.write, which completes with no egress."
        type: string
        default: "Create three tasks for my launch: draft the announcement, schedule the social posts, and set up the landing page"

permissions:
  contents: read

jobs:
  serve-run-completion:
    name: serve + Claude planner -> run completes (live stack)
    runs-on: ubuntu-latest
    timeout-minutes: 25

    services:
      postgres:
        image: pgvector/pgvector:pg15
        env:
          POSTGRES_USER: aindy_test
          POSTGRES_PASSWORD: aindy_test
          POSTGRES_DB: aindy_test
        ports:
          - 5433:5432
        options: >-
          --health-cmd "pg_isready -U aindy_test -d aindy_test"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 20
      redis:
        image: redis:7-alpine
        ports:
          - 6380:6379
        options: >-
          --health-cmd "redis-cli ping"
          --health-interval 5s
          --health-timeout 3s
          --health-retries 20
      mongo:
        image: mongo:7
        ports:
          - 27017:27017
        options: >-
          --health-cmd "mongosh --quiet --eval 'db.adminCommand({ ping: 1 })'"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 20

    env:
      DATABASE_URL: postgresql+psycopg2://aindy_test:aindy_test@localhost:5433/aindy_test
      REDIS_URL: redis://localhost:6380
      AINDY_REDIS_URL: redis://localhost:6380
      MONGO_URL: mongodb://localhost:27017/aindy_serve_smoke
      # Load the 17 app plugins (serve otherwise setdefaults AINDY_BOOT_MODE=runtime-only).
      AINDY_BOOT_MODE: app-profile
      AINDY_APP_PLUGIN_MANIFEST: ./aindy_plugins.json
      # Planner backend + objective (dispatch inputs; anthropic_chat is the default).
      AINDY_AGENT_PLANNER_BACKEND: ${{ github.event.inputs.planner_backend || 'anthropic_chat' }}
      AINDY_CLAUDE_PLANNER_MODEL: claude-sonnet-5
      RUN_OBJECTIVE: "${{ github.event.inputs.objective || 'Create three tasks for my launch: draft the announcement, schedule the social posts, and set up the landing page' }}"
      # The nodus worker cold-starts the 17-app stack (~12s) per execution, so the default
      # 30s wall-clock budget is too tight for a multi-step plan (run reached `executing`
      # then failed "Nodus script exceeded 30000ms"). 1.7.0 made the budget configurable;
      # give it headroom. Durable fix is a warm worker (runtime NODUS-WARMPOOL-1).
      AINDY_NODUS_MAX_EXECUTION_MS: "180000"
      ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
      OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
      DEEPSEEK_API_KEY: ${{ secrets.DEEPSEEK_API_KEY }}
      SECRET_KEY: ci-serve-smoke-secret-key-at-least-32-chars
      AINDY_API_KEY: ci-serve-smoke-api-key
      PERMISSION_SECRET: ci-serve-smoke-permission-secret-32-chars
      AINDY_HOST: 127.0.0.1
      AINDY_PORT: "8000"

    steps:
      - name: Checkout
        uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3
        with:
          fetch-depth: 0

      - name: Setup Python
        uses: actions/setup-python@a309ff8b426b58ec0e2a45f0f869d46889d02405 # v6.2.0
        with:
          python-version: "3.11"

      - name: Install app (pulls pinned aindy-runtime from PyPI)
        run: |
          python -m pip install --upgrade pip
          python -m pip install -e .[test]

      - name: Preflight — required secret present (anthropic_chat only)
        if: ${{ (github.event.inputs.planner_backend || 'anthropic_chat') == 'anthropic_chat' }}
        run: |
          if [ -z "${ANTHROPIC_API_KEY:-}" ]; then
            echo "::error::missing repo secret ANTHROPIC_API_KEY — add it in Settings > Secrets and variables > Actions"
            exit 1
          fi
          echo "ANTHROPIC_API_KEY present"

      - name: Preflight — Anthropic reachable via SDK (anthropic_chat only)
        if: ${{ (github.event.inputs.planner_backend || 'anthropic_chat') == 'anthropic_chat' }}
        # A curl GET can connect while the SDK's POST /v1/messages is blocked (RTR-1-NODUS-APPTOOL-500:
        # the earlier run passed a curl preflight but the planner still got "Connection error"). Exercise
        # the SDK egress path the planner actually uses, so an egress block fails HERE, clearly.
        run: |
          python - <<'PY'
          import os, sys, anthropic
          client = anthropic.Anthropic(api_key=os.environ["ANTHROPIC_API_KEY"])
          try:
              client.models.list(limit=1)
          except anthropic.APIConnectionError as e:
              print(f"::error::runner cannot reach api.anthropic.com via the SDK (egress blocked — RTR-1-NODUS-APPTOOL-500). anthropic_chat cannot run on GitHub-hosted runners: {e}")
              sys.exit(1)
          except anthropic.AuthenticationError as e:
              print(f"::error::ANTHROPIC_API_KEY rejected (401): {e}")
              sys.exit(1)
          print("Anthropic SDK reachable and key accepted")
          PY

      - name: Build schema (create_all from live metadata)
        # The runtime schema guard refuses to boot on drift; a fresh DB needs the full
        # runtime+app schema built from live metadata (dodges app-alembic/runtime-guard
        # ordering — the APP-DEPLOY-1 concern is out of scope for this smoke).
        run: |
          python - <<'PY'
          import AINDY.main  # registers runtime + 17 app models onto Base.metadata
          from AINDY.db.database import Base, engine
          from sqlalchemy import text, inspect
          with engine.begin() as c:
              c.execute(text("CREATE EXTENSION IF NOT EXISTS vector"))
          Base.metadata.create_all(bind=engine)
          t = set(inspect(engine).get_table_names())
          print(f"[schema] {len(t)} tables")
          for p in ("agent_runs", "users", "tasks", "agent_steps"):
              assert p in t, f"missing table {p}"
          print("[schema] core tables present")
          PY

      - name: Boot serve + drive one run to completion
        run: |
          set -e
          aindy-runtime serve > /tmp/serve.log 2>&1 &
          echo "serve pid $!"

          echo "waiting for serve readiness..."
          up=""
          for i in $(seq 1 60); do
            code=$(curl -s -o /dev/null -w '%{http_code}' --max-time 4 http://127.0.0.1:8000/api/version || echo 000)
            if [ "$code" = "200" ]; then up=yes; echo "serve up after ~$((i*2))s"; break; fi
            sleep 2
          done
          if [ "$up" != yes ]; then echo "::error::serve did not become ready"; tail -50 /tmp/serve.log; exit 1; fi

          python - <<'PY'
          import urllib.request, json
          r = json.load(urllib.request.urlopen("http://127.0.0.1:8000/api/version", timeout=10)).get("runtime", {})
          print("boot_profile", r.get("boot_profile"), "apps", r.get("app_plugin_count"))
          assert r.get("app_plugins_loaded") and r.get("app_plugin_count") == 17, "app profile not loaded"
          PY

          python - <<'PY'
          import json, os, sys, time, uuid, urllib.error, urllib.request
          BASE = "http://127.0.0.1:8000"
          def req(method, path, token=None, body=None):
              data = json.dumps(body).encode() if body is not None else None
              r = urllib.request.Request(BASE + path, data=data, method=method)
              r.add_header("Content-Type", "application/json")
              if token: r.add_header("Authorization", "Bearer " + token)
              try:
                  with urllib.request.urlopen(r, timeout=60) as resp:
                      return resp.status, json.loads(resp.read().decode() or "{}")
              except urllib.error.HTTPError as e:
                  try: return e.code, json.loads(e.read().decode() or "{}")
                  except Exception: return e.code, {}
          def inner(d): return d.get("data") if isinstance(d, dict) and isinstance(d.get("data"), dict) else d
          def rid_of(d):
              i = inner(d) or {}
              return i.get("run_id") or i.get("id") or (i.get("execution_record") or {}).get("run_id")
          def status_of(d): return (inner(d) or {}).get("status")

          email = f"ci-{uuid.uuid4().hex[:8]}@example.com"
          st, tok = req("POST", "/auth/register", body={"email": email, "password": "Ci-pass-123!", "username": "ci"})
          token = tok.get("access_token"); print("register", st, "token", bool(token)); assert token, tok

          goal = os.environ.get("RUN_OBJECTIVE") or "Create three tasks for my launch"
          st, run = req("POST", "/apps/agent/run", token, {"goal": goal})
          rid = rid_of(run); print("create", st, "status", status_of(run), "run_id", rid); assert rid, run

          TERMINAL = {"completed", "failed", "verify_failed", "dead_letter", "cancelled"}
          approved = False; final = None
          for i in range(90):
              st, r = req("GET", f"/apps/agent/runs/{rid}", token)
              s = status_of(r); print(f"[{i:02d}] {s}")
              if s in ("pending_approval", "awaiting_approval") and not approved:
                  ast, _ = req("POST", f"/apps/agent/runs/{rid}/approve", token); print("   approve ->", ast); approved = True
              elif s in TERMINAL:
                  final = s; break
              time.sleep(3)

          st, r = req("GET", f"/apps/agent/runs/{rid}", token)
          st2, steps = req("GET", f"/apps/agent/runs/{rid}/steps", token)
          i = inner(r)
          slist = steps.get("data", steps) if isinstance(steps, dict) else steps
          print("FINAL status", i.get("status"), "steps", i.get("steps_completed"), "/", i.get("steps_total"))
          for s in (slist if isinstance(slist, list) else []):
              print("  -", s.get("tool") or s.get("name"), ":", s.get("status"))
          assert i.get("status") == "completed", f"run did not complete: status={i.get('status')}"
          print("VERDICT: ACTED — completed")
          PY

      - name: Serve log (on failure)
        if: failure()
        run: tail -120 /tmp/serve.log || true
